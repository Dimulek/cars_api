name: cars_api
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.2.5 <4.0.0'

dependencies:
  
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  provider: ^6.1.1
  get_it: ^7.6.7
  dio: ^5.4.0
  pretty_dio_logger: ^1.3.1
  go_router: ^13.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
